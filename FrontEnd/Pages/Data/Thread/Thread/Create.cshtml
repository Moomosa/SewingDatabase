@page
@model FrontEnd.Pages.Data.Thread.Thread.CreateModel

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Thread</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Thread.ThreadTypeID" class="control-label"></label>
                <select asp-for="Thread.ThreadTypeID" class ="form-control form-select" >
                    <option value=""></option>
                    @foreach(var threadType in Model.ThreadTypes)
                    {
                        <option value="@threadType.ID">@threadType.Name</option>
                    }
                </select>
                <span asp-validation-for="Thread.ThreadType.ID" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label asp-for="Thread.ColorID" class="control-label"></label>
                <select asp-for="Thread.ColorID" class ="form-control form-select" >
                    <option value=""></option>
                    @foreach(var threadColor in Model.ThreadColors)
                    {
                        <option value="@threadColor.ID">@threadColor.Color</option>
                    }
                </select>
                <span asp-validation-for="Thread.Color.ID" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label asp-for="Thread.Quantity" class="control-label"></label>
                <input asp-for="Thread.Quantity" class="form-control" />
                <span asp-validation-for="Thread.Quantity" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
